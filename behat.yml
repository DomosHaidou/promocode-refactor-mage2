default:
    suites:
        core_features:
            paths:    [ %paths.base%/features/core ]
            contexts: [ CoreDomainContext ]
        ui_features:
            paths:    [ %paths.base%/features/ui ]
            contexts: [ UIContext ]